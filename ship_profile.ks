// Ship profile

Ship name: 
UID:

// parts:

set transferengine to "transferengine".
set lander to "lander".

function build_ship_profile{
	parameter missionprofile.

	set ship:name to ship:name + 


}